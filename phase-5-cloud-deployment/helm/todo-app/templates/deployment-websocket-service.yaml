apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todo-app.fullname" . }}-websocket-service
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: websocket-service
spec:
  replicas: {{ .Values.replicaCount.websocketService | default 1 }}
  selector:
    matchLabels:
      {{- include "todo-app.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: websocket-service
  template:
    metadata:
      labels:
        {{- include "todo-app.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: websocket-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "websocket-service"
        dapr.io/app-port: "8004"
        dapr.io/config: "dapr-config"
        dapr.io/log-level: "info"
        dapr.io/log-as-json: "true"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: websocket-service
        image: "{{ .Values.image.websocketService.repository }}:{{ .Values.image.websocketService.tag }}"
        imagePullPolicy: {{ .Values.image.websocketService.pullPolicy }}
        ports:
        - name: http
          containerPort: 8004
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: {{ .Values.healthProbes.websocketService.liveness.initialDelaySeconds | default 30 }}
          periodSeconds: {{ .Values.healthProbes.websocketService.liveness.periodSeconds | default 15 }}
          timeoutSeconds: {{ .Values.healthProbes.websocketService.liveness.timeoutSeconds | default 5 }}
          failureThreshold: {{ .Values.healthProbes.websocketService.liveness.failureThreshold | default 3 }}
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          initialDelaySeconds: {{ .Values.healthProbes.websocketService.readiness.initialDelaySeconds | default 10 }}
          periodSeconds: {{ .Values.healthProbes.websocketService.readiness.periodSeconds | default 10 }}
          timeoutSeconds: {{ .Values.healthProbes.websocketService.readiness.timeoutSeconds | default 5 }}
          failureThreshold: {{ .Values.healthProbes.websocketService.readiness.failureThreshold | default 3 }}
        resources:
          limits:
            cpu: {{ .Values.resources.websocketService.limits.cpu | default "500m" }}
            memory: {{ .Values.resources.websocketService.limits.memory | default "512Mi" }}
          requests:
            cpu: {{ .Values.resources.websocketService.requests.cpu | default "100m" }}
            memory: {{ .Values.resources.websocketService.requests.memory | default "256Mi" }}
        envFrom:
        - configMapRef:
            name: {{ include "todo-app.fullname" . }}-config
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: DATABASE_URL
        - name: BETTER_AUTH_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: BETTER_AUTH_SECRET
        - name: DAPR_HTTP_PORT
          value: "3500"
        - name: PUBSUB_NAME
          value: "kafka-pubsub"
        - name: STATE_STORE_NAME
          value: "statestore"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
