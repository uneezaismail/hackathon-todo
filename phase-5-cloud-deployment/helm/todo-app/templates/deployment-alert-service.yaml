apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todo-app.fullname" . }}-alert-service
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: alert-service
spec:
  replicas: {{ .Values.replicaCount.alertService | default 1 }}
  selector:
    matchLabels:
      {{- include "todo-app.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: alert-service
  template:
    metadata:
      labels:
        {{- include "todo-app.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: alert-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "alert-service"
        dapr.io/app-port: "8002"
        dapr.io/config: "dapr-config"
        dapr.io/log-level: "info"
        dapr.io/log-as-json: "true"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: alert-service
        image: "{{ .Values.image.alertService.repository }}:{{ .Values.image.alertService.tag }}"
        imagePullPolicy: {{ .Values.image.alertService.pullPolicy }}
        ports:
        - name: http
          containerPort: 8002
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: {{ .Values.healthProbes.alertService.liveness.initialDelaySeconds | default 30 }}
          periodSeconds: {{ .Values.healthProbes.alertService.liveness.periodSeconds | default 15 }}
          timeoutSeconds: {{ .Values.healthProbes.alertService.liveness.timeoutSeconds | default 5 }}
          failureThreshold: {{ .Values.healthProbes.alertService.liveness.failureThreshold | default 3 }}
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          initialDelaySeconds: {{ .Values.healthProbes.alertService.readiness.initialDelaySeconds | default 10 }}
          periodSeconds: {{ .Values.healthProbes.alertService.readiness.periodSeconds | default 10 }}
          timeoutSeconds: {{ .Values.healthProbes.alertService.readiness.timeoutSeconds | default 5 }}
          failureThreshold: {{ .Values.healthProbes.alertService.readiness.failureThreshold | default 3 }}
        resources:
          limits:
            cpu: {{ .Values.resources.alertService.limits.cpu | default "500m" }}
            memory: {{ .Values.resources.alertService.limits.memory | default "512Mi" }}
          requests:
            cpu: {{ .Values.resources.alertService.requests.cpu | default "100m" }}
            memory: {{ .Values.resources.alertService.requests.memory | default "256Mi" }}
        envFrom:
        - configMapRef:
            name: {{ include "todo-app.fullname" . }}-config
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: DATABASE_URL
        - name: BETTER_AUTH_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: BETTER_AUTH_SECRET
        - name: DAPR_HTTP_PORT
          value: "3500"
        - name: PUBSUB_NAME
          value: "kafka-pubsub"
        - name: STATE_STORE_NAME
          value: "statestore"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
